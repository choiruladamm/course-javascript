// 3 fundamental function in manipulate array

const names = ["Jessica", "Alex", "John", "Jessica", "Jessica"];

// show array in list format + 1 in every single data
// names.map((name) => {
//   return name + "1"; // same way console.log(name + "1");`
// });

// show array in list format
// names.map((name) => {
//   console.log(name);
// });

// show array in list format without "Jessica"
// names.filter((name) => {
//   return name !== "Jessica";
// });

// example show data array in list format react
// names.map((name) => {
//   return <li>{name}</li>;
// });
